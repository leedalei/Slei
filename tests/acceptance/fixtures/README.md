# Slei MVP Acceptance Fixtures

The MVP acceptance suite uses inline deterministic fixtures in
`apps/desktop/e2e/mvp-acceptance.spec.ts` so it can run without a cloud service,
developer Node path or live runtime credentials.

