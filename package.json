{
  "name": "slei",
  "private": true,
  "scripts": {
    "dev:reset": "node scripts/dev-reset.mjs",
    "verify:architecture": "node scripts/verify-architecture-guardrails.mjs",
    "test": "pnpm -r test && pnpm test:guardrails",
    "test:guardrails": "node --test scripts/verify-architecture-guardrails.test.mjs scripts/verify-release-workflow.test.mjs",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
