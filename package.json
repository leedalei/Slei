{
  "name": "slei",
  "private": true,
  "scripts": {
    "dev:reset": "node scripts/dev-reset.mjs",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
