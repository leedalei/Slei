{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Slei Webview may only call narrow broker commands; daemon transport and OS actions stay in Rust.",
  "windows": ["main"],
  "permissions": []
}
