{
  "name": "@slei/desktop",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "vitest run",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@slei/i18n": "workspace:*",
    "@slei/protocol-client": "workspace:*"
  }
}
